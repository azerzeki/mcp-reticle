/// Default demo session ID
pub const DEFAULT_DEMO_SESSION_ID: &str = "demo-session-12345";

/// Default delay between demo messages (milliseconds)
pub const DEFAULT_DEMO_MESSAGE_DELAY_MS: u64 = 10;

/// Default batch size for progress logging
pub const DEFAULT_DEMO_PROGRESS_BATCH: usize = 10;

/// Default session startup delay (milliseconds)
pub const DEFAULT_SESSION_STARTUP_DELAY_MS: u64 = 100;
